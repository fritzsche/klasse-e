\setcounter{section}{5}
\section{Reihen- und Parallelschaltung von Bauelementen}

\subsection{Widerstand in Reihen- und Parallelschaltung}

\begin{sol}{ED104}
Wir rechnen:   $$ R_G = \frac{R_1\cdot R_2}{R_1 + R_2}  = \frac{\SI{100}{\ohm}\cdot \SI{400}{\ohm}}{\SI{100}{\ohm} + \SI{400}{\ohm}}=\frac{\SI{400}{\ohm}}{5}=\SI{80}{\ohm} $$.
\end{sol}


\begin{sol}{ED105}
Wir rechnen:   $$ R_G = \frac{R_1\cdot R_2}{R_1 + R_2}  = \frac{\SI{50}{\ohm}\cdot \SI{200}{\ohm}}{\SI{50}{\ohm} + \SI{200}{\ohm}}=\frac{\SI{10000}{\ohm}}{250}=\SI{40}{\ohm} $$.
\end{sol}

\begin{sol}{ED106}
Wie parallelen Widerstände haben alle den selben Widerstand. Also können wir rechnen:   $$ \SI{1.7}{\kilo\ohm}\cdot 3=\SI{5.1}{\kilo\ohm}$$.
\end{sol}

\begin{sol}{ED107}
Wie parallelen Widerstände haben alle den selben Widerstand. Durch jeden der Widerstände geht ein Teil des Stroms:  
 $$ \SI{1}{\watt}\cdot 3=\SI{3}{\watt}$$.
\end{sol}

\begin{sol}{ED108}
In der Regel ist es zunächst einfacher die Widerstände in Reihe zu berechnen: 
 $$ R_1 + R_2 = \SI{500}{\ohm} + \SI{500}{\ohm} = \SI{1}{\kilo\ohm} $$.
 Also gilt $R_1 + R_2 = R_3$ für die verbleibende  Parallelschaltung:wir rechnen vereinfacht.
 $$ \frac{\SI{1000}{\ohm}}{2} = \SI{500}{\ohm} $$ 
\end{sol}


\begin{sol}{ED109}
In der Regel ist es zunächst einfacher die Widerstände in Reihe zu berechnen: 
 $$ R_1 + R_2 = \SI{500}{\ohm} + \SI{1,5}{\kilo\ohm} = \SI{2}{\kilo\ohm} $$.
 Also gilt $R_1 + R_2 = R_3$ für die verbleibende  Parallelschaltung:wir rechnen vereinfacht.
 $$ \frac{\SI{2000}{\ohm}}{2} = \SI{1}{\kilo\ohm} $$ 
\end{sol}

\begin{sol}{ED110}
$R_2$ und $R_3$ haben den selben Widerstand, wir können also vereinfacht rechnen:
 $$ \frac{\SI{1000}{\ohm}}{2} = \SI{500}{\ohm} $$ 

 Jetzt nur noch die verbleibende Reihenschaltung aus $R_1$ und der Parallelschaltung von $R_2$ und $R_3$ die wir mit $\SI{500}{\ohm}$ errechnet haben:
 $\SI{500}{\ohm}+ \SI{500}{\ohm} = \SI{1000}{\ohm} $
\end{sol}


\begin{sol}{ED111}
$R_2$ und $R_3$ haben den selben Widerstand, wir können also vereinfacht rechnen:
 $$ \frac{\SI{2000}{\ohm}}{2} = \SI{1000}{\ohm} $$ 

 Jetzt nur noch die verbleibende Reihenschaltung aus $R_1$ und der Parallelschaltung von $R_2$ und $R_3$ die wir mit $\SI{500}{\ohm}$ errechnet haben:
 $\SI{1}{\kilo\ohm}+ \SI{1}{\kilo\ohm} = \SI{2}{\kilo\ohm} $
\end{sol}



\begin{sol}{ED112}
Wir rechnen:
 $$
 \frac{R_2\cdot R_3}{R_2 + R_3} 
 = \frac{\SI{3}{\kilo\ohm}\cdot \SI{1,5}{\kilo\ohm}}{\SI{3}{\kilo\ohm} + \SI{1,5}{\kilo\ohm}} = \frac{\SI{4,5}{\kilo\ohm}}{\SI{4,5}{\kilo\ohm}} = \SI{1}{\kilo\ohm}$$
 
 Jetzt nur noch die verbleibende Reihenschaltung aus $R_1$ und der Parallelschaltung von $R_2$ und $R_3$ die wir mit $\SI{1000}{\ohm}$ errechnet haben:
 $\SI{1}{\kilo\ohm}+ \SI{1}{\kilo\ohm} = \SI{2}{\kilo\ohm} $
\end{sol}


\begin{sol}{ED113}
Wir rechnen:
 $$
 \frac{R_2\cdot R_3}{R_2 + R_3} 
 = \frac{\SI{2,5}{\kilo\ohm}\cdot \SI{0,5}{\kilo\ohm}}{\SI{2,5}{\kilo\ohm} + \SI{0,5}{\kilo\ohm}} = \frac{\SI{1,25}{\kilo\ohm}}{\SI{3}{\kilo\ohm}} \approx \SI{0,4}{\kilo\ohm}$$
 

 $$
 \frac{R_1\cdot 0,4}{R_1 + 0,4} 
 = \frac{\SI{10}{\kilo\ohm}\cdot \SI{0,4}{\kilo\ohm}}{\SI{10}{\kilo\ohm} + \SI{0,4}{\kilo\ohm}} = \frac{\SI{4}{\kilo\ohm}}{\SI{10,4}{\kilo\ohm}} \approx \SI{0,38}{\kilo\ohm}$$

 Jetzt nur noch die verbleibende Reihenschaltung aus $R_4$ den parallelen Widerständen::
 $\SI{0,38}{\kilo\ohm}+ \SI{0,6}{\kilo\ohm} \approx \SI{1}{\kilo\ohm} $
\end{sol}



\begin{ohmchapter}

    In der Formelsammlung finden wir um Kapitel Widerstände alles was wir wissen müssen.
    Es geht hier um Parallelschaltung und Reihenschaltung und bevor wir mit den Formel starten zunächst die Überlegung, was passieren sollte, wenn wir mehrere Widerstände in Reihe (also Hintereinander) schalten. Der Strom muss durch beide Widerstände und damit entspricht der Widerstand der Summe der einzelnen Widerstände. 

    \textbf{Reihenschaltung}
    $$ R_G = R_1+ R_2 + R_3 + \ldots + R_N$$
    oder für 2 Widerstände Gleichrichter
    $$ R_G = R_1+ R_2 $$

   In Parallelschaltung hat der Strom quasi die Auswahl durch welchen widerstand er fließen will. Haben alle Widerstände den selben Wert können wir also durch die Anzahl der Widerstände teilen. 

     \textbf{Parallelschaltung}. 
     Haben $N$ Parallelgeschalteten Widerstände den gleichen Wert $R_1$, so gilt offenbar:
   $$ R_G = \frac{R_1}{N} $$ oder im Fall von 2 Widerständen:
  $$ R_G = \frac{R_1}{2} $$.
   Haben wir nur genau 2 Widerstände $R_1$ und $R_2$ in Parallelschaltung, so gilt laut Formelsammlung:
   $$ R_G = \frac{R_1\cdot R_2}{R_1 + R_2}. $$

  
  
\end{ohmchapter}


\subsection{Widerstandsnetzwerke I}

\begin{sol}{ED114}
Wir vereinfachen Stück für Stück die Schaltung. Schauen wir uns zunächst nur diesen Teil der Schaltung an:
     \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}

         \draw (0,0) -- ++(1,0) coordinate(start)  to[short,*-] ++(0,-4) 
         to[R=$\SI{50}{\ohm}$] ++(4,0)
         to[R,l_=$\SI{50}{\ohm}$,-*] ++(0,4) coordinate(ende) 
         -- ++(1,0)
         (ende) -- ++(0,1) 
         (start) to[R,l=$\SI{100}{\ohm}$] (ende);
        \end{circuitikz}
    \end{center}
Es fällt auf, dass wir die beiden $\SI{50}{\ohm}$ in Reihenschaltung einfach durch einen $\SI{100}{\ohm}$
Widerstand ersetzen können:

     \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}

         \draw (0,0) -- ++(1,0) coordinate(start)  to[short,*-] ++(0,-4) 
         to[R=$\SI{100}{\ohm}$] ++(4,0)
         -- ++(0,4) coordinate(ende) 
         -- ++(1,0)
         (ende) -- ++(0,1) 
         (start) 
         to[R,l=$\SI{100}{\ohm}$,-*] (ende);
        \end{circuitikz}
    \end{center}
Nun haben wir eine Parallelschaltung von zwei gleichen  $\SI{100}{\ohm}$ Widerständen.

     \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}
         \draw (0,0) to[short,-*] ++(1,0) coordinate(start)  to[R=$\SI{50}{\ohm}$,-*] ++(4,0) coordinate(ende) -- ++(1,0)
         (ende) -- ++(0,1) 
         ;
        \end{circuitikz}
    \end{center}
Schauen wir den Schaltplan weiter an, so erkennen wir noch eine Kombination aus 
$\SI{50}{\ohm}$ und $\SI{100}{\ohm}$  Widerstände die auch insgesamt einen Widerstand von $\SI{50}{\ohm}$ haben sollte.
Es bleibt daher übrig:
     \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}
         \draw (0,0) 
          to[short,o-] ++(1,0)
          to[R=$\SI{100}{\ohm}$,o-,-*] ++(4,0)  
          to[R=$\SI{50}{\ohm}$,-*] ++(4,0)  
          to[R=$\SI{50}{\ohm}$,-*] ++(4,0)
          -- ++(0,-6) -- ++(-4,0)
          to[R=$\SI{50}{\ohm}$] ++(-4,0)
          to[short,-o] ++(-5,0)
          
         ;
        \end{circuitikz}
    \end{center}   
 In Summe haben wir den Widerstand: 
 $\SI{100}{\ohm}+
 \SI{50}{\ohm}+\SI{50}{\ohm}+\SI{50}{\ohm} = \SI{250}{\ohm}$. 

\end{sol}

\begin{sol}{ED115}
  Analog zur Frage \qref{ED114}. Wir vereinfachen diesen Teil der Schaltung:
     \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}

         \draw (0,0) -- ++(1,0) coordinate(start)  to[short,*-] ++(0,-4) 
         to[R=$\SI{100}{\ohm}$] ++(4,0)
         to[R,l_=$\SI{100}{\ohm}$,-*] ++(0,4) coordinate(ende) 
         -- ++(1,0)
         (ende) -- ++(0,1) 
         (start) to[R,l=$\SI{200}{\ohm}$] (ende);
        \end{circuitikz}
    \end{center}  
Wird zu:
     \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}

         \draw (0,0) -- ++(1,0) coordinate(start)  to[short,*-] ++(0,-4) 
         to[R=$\SI{200}{\ohm}$] ++(4,0)
         -- ++(0,4) coordinate(ende) 
         -- ++(1,0)
         (ende) -- ++(0,1) 
         (start) 
         to[R,l=$\SI{200}{\ohm}$,-*] (ende);
        \end{circuitikz}
    \end{center}
    Weiter:
         \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}
         \draw (0,0) to[short,-*] ++(1,0) coordinate(start)  to[R=$\SI{100}{\ohm}$,-*] ++(4,0) coordinate(ende) -- ++(1,0)
         (ende) -- ++(0,1) 
         ;
        \end{circuitikz}
    \end{center}
Es bleibt daher übrig:
     \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}
         \draw (0,0) 
          to[short,o-] ++(1,0)
          to[R=$\SI{200}{\ohm}$,o-,-*] ++(4,0)  
          to[R=$\SI{100}{\ohm}$,-*] ++(4,0)  
          to[R=$\SI{100}{\ohm}$,-*] ++(4,0)
          -- ++(0,-6) -- ++(-4,0)
          to[R=$\SI{150}{\ohm}$] ++(-4,0)
          to[short,-o] ++(-5,0)
          
         ;
        \end{circuitikz}
    \end{center}      
 In Summe haben wir den Widerstand: 
 $\SI{200}{\ohm}+
 \SI{100}{\ohm}+\SI{100}{\ohm}+\SI{150}{\ohm} = \SI{150}{\ohm}$.     
\end{sol}

\begin{sol}{ED116}
   Analog zur Frage \qref{ED114} und Frage \qref{ED115}. 
Wir vereinfachen diesen Teil der Schaltung:
     \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}

         \draw (0,0) -- ++(1,0) coordinate(start)  to[short,*-] ++(0,-4) 
         to[R=$\SI{200}{\ohm}$] ++(4,0)
         to[R,l_=$\SI{200}{\ohm}$,-*] ++(0,4) coordinate(ende) 
         -- ++(1,0)
         (ende) -- ++(0,1) 
         (start) to[R,l=$\SI{400}{\ohm}$] (ende);
        \end{circuitikz}
    \end{center}  
Zunächst:
     \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}

         \draw (0,0) -- ++(1,0) coordinate(start)  to[short,*-] ++(0,-4) 
         to[R=$\SI{400}{\ohm}$] ++(4,0)
         -- ++(0,4) coordinate(ende) 
         -- ++(1,0)
         (ende) -- ++(0,1) 
         (start) 
         to[R,l=$\SI{400}{\ohm}$,-*] (ende);
        \end{circuitikz}
    \end{center}
    Weiter:
         \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}
         \draw (0,0) to[short,-*] ++(1,0) coordinate(start)  to[R=$\SI{200}{\ohm}$,-*] ++(4,0) coordinate(ende) -- ++(1,0)
         (ende) -- ++(0,1) 
         ;
        \end{circuitikz}
    \end{center}

Es bleibt daher übrig:
     \begin{center}           
     \begin{circuitikz}[scale=0.5]
        \ctikzset{inductors/scale=0.8, inductor=american}
        \ctikzset{capacitors/scale=0.8}
        \ctikzset{european resistors}
         \draw (0,0) 
          to[short,o-] ++(1,0)
          to[R=$\SI{400}{\ohm}$,o-,-*] ++(4,0)  
          to[R=$\SI{200}{\ohm}$,-*] ++(4,0)  
          to[R=$\SI{200}{\ohm}$,-*] ++(4,0)
          -- ++(0,-6) -- ++(-4,0)
          to[R=$\SI{150}{\ohm}$] ++(-4,0)
          to[short,-o] ++(-5,0)
          
         ;
        \end{circuitikz}
    \end{center}   
 In Summe haben wir den Widerstand: 
 $\SI{400}{\ohm}+
 \SI{200}{\ohm}+\SI{200}{\ohm}+\SI{150}{\ohm} = \SI{950}{\ohm}$. 


\end{sol}

\begin{ohmchapter}
In diesem Kapitel wenden wir nur an was wir bereits im vorherigen Kapitel zu Reihen- und Parallelschaltung von Widerständen bereits gelernt haben.
Hierbei ist es wichtig in der richtigen Reihenfolge vorgehen: Die Fragen sind so konstruiert, dass sie mit den vereinfachten Formeln zu lösen sind.
\end{ohmchapter}

\subsection{Spannungsteiler I}
\begin{sol}{ED101}
 Laut Aufgabe gilt $R_1 = 5 \cdot R_2$.
 Im Spannungsteiler sind die Spannungen im selben Verhältnis wir die Widerstände.
 Also gilt $U_1 = 5 \cdot U_2 $.
\end{sol}

\begin{sol}{ED102}
 Analog zur Frage \qref{ED101} Laut Aufgabe gilt $R_1 = \frac{R_2}{6}$.
 Im Spannungsteiler sind die Spannungen im selben Verhältnis wir die Widerstände.
 Also gilt $U_1 = \frac{U_2}{6}$.
\end{sol}

\begin{sol}{ED103}
Mit den Werten der Aufgabenstellung $R_1 = \SI{10}{\kilo\ohm}$ und $R_2 = \SI{20}{\kilo\ohm}$ gilt, dass wir einen Gesamtwiderstand haben $$R_\text{gesamt} = R_1+R_2 = \SI{10}{\kilo\ohm} + \SI{20}{\kilo\ohm} = \SI{30}{\kilo\ohm} $$. Der Widerstand $R_2$ ist also $\frac{2}{3}$ des Gesamtwiderstandes. Analog zu den vorherigen Fragen: dieses Verhältnis der Widerstände besteht auch für die Spannungen, d.g. $U_2 = \frac{2}{3} \cdot U_\text{gesamt} = \frac{2}{3} \cdot \SI{9}{\volt}= \SI{6}{\volt}$.
\end{sol}


\begin{ohmchapter}
\end{ohmchapter}